/// @description Draw given text
draw_self(); //Otherwise the choiceBox won't show up
draw_text_ext(bbox_left + 32, bbox_top + 32, string_hash_to_newline(myMessage), 16, maxLength); //Draws the text given the padding and width seperation we've set up

